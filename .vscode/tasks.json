{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run OpenGL",
            "type": "shell",
            "command": "clang++ \"${file}\" -framework OpenGL -framework GLUT -o \"${fileDirname}/${fileBasenameNoExtension}\" && \"${fileDirname}/${fileBasenameNoExtension}\"",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}